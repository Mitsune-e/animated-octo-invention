using System;
using System.Collections.Generic;
using System.Linq;

// Array - a fixed size list of variables
string[] fellowship = { "Frodo", "Sam", "Gandalf", "Aragorn", "Legolas" };

// Console.WriteLine(fellowship[0]);
// Console.WriteLine(fellowship.Length);

// Console.WriteLine(fellowship[4]);
// fellowship[4] = "Boromir";
// Console.WriteLine(fellowship[4]);


foreach (var item in fellowship)
{
    Console.WriteLine(item);
}

for (int i = 0; i < fellowship.Length; i++)
{
    if(fellowship[i] == "Gandalf")
    {
        //Console.WriteLine(fellowship[i] + " is a powerful wizard. He leads the way!");
        continue;
    }
    Console.WriteLine($"{fellowship[i]} at fellowship[{i}] has joined the Fellowship!");
}

Console.WriteLine();
Console.WriteLine();
for (int i = 0; i < fellowship.Length; i++)
{
    for (int j = i + 1; j < fellowship.Length; j++)
    {
        Console.WriteLine($"{fellowship[i]} is paired with {fellowship[j]}.");
    }
}

int age = 10;

while (age > 0)
{
    Console.WriteLine(age);
    if(age == 5) 
        break;
    age--;
}


// List<string> gandalfInventory = new List<string>();

// gandalfInventory.Add("Pipe");
// gandalfInventory.Add("Glamdring");
// gandalfInventory.Add("Wizard Staff");

List<string> gandalfInventory = new List<string> 
{
    "Pipe", "Glamdring", "Wizard Staff"
};

foreach (var item in gandalfInventory)
{   
    Console.WriteLine(item);    
}

Console.WriteLine();    

bool hasPipe = gandalfInventory.Contains("Pipe");
Console.WriteLine($"gandalfInventory hasPipe:{hasPipe}");   

gandalfInventory.Remove("Pipe");
foreach (var item in gandalfInventory)
{   
    Console.WriteLine(item);    
}
Console.WriteLine($"gandalfInventory hasPipe:{gandalfInventory.Contains("Pipe")}");   






gandalfInventory.Add("Pipe");

List<string> sortedInventory = gandalfInventory.OrderBy(name => name).ToList();
foreach (var item in sortedInventory)
{   
    Console.WriteLine(item);    
}

string ourItem = gandalfInventory.FirstOrDefault(item => item == "Cloak");

Console.WriteLine($"ourItem:<{ourItem}>");
